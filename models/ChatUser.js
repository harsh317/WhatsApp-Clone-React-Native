class ChatUser {
  constructor(id, name, description, profileImageUrl, userId) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.profileImageUrl = profileImageUrl;
    this.userId = userId;
  }
}

export default ChatUser;
