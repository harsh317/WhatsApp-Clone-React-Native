import React from "react";
import { HeaderButton } from "react-navigation-header-buttons";

const CustomHeaderButton = (props) => {
  return <HeaderButton {...props} iconSize={23} color="white" />;
};

export default CustomHeaderButton;
